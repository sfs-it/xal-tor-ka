{ "description": "Shared DB instance status as JSON (installed/running/version/port). No side effects.",
  "timeout_seconds": 30,
  "params": { "engine": { "required": true, "enum": ["pg", "mysql"] } } }
