{
  "description": "Replace a vhost's nginx.conf (validated with 'nginx -t' in the running web container, reverted on error), then reload",
  "timeout_seconds": 60,
  "params": {
    "name":    { "required": true, "pattern": "[a-z][a-z0-9-]{1,30}" },
    "vhost":   { "required": true, "pattern": "[a-z][a-z0-9-]{1,30}" },
    "content": { "required": true }
  }
}
