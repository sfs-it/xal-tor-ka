{ "description": "Start an ephemeral Adminer container for the shared DB engine, on a random token alias (xtk-hosting). Prints connection.",
  "timeout_seconds": 120,
  "params": { "engine": { "required": true, "enum": ["pg", "mysql"] }, "token": { "required": true, "pattern": "[a-f0-9]{8,40}" } } }
