{ "description": "Write a site's db.env (KEY=VALUE lines from admin) for the php container; content arrives only as env.",
  "timeout_seconds": 30,
  "params": { "name": { "required": true, "pattern": "[a-z][a-z0-9-]{1,30}" }, "content": { "required": true } } }
