{ "description": "Delete a site OS user ONLY if orphaned (its /opt/sites/<name> is gone); else refuse",
  "timeout_seconds": 20,
  "params": { "name": { "required": true, "pattern": "[a-z][a-z0-9-]{1,30}" } } }
