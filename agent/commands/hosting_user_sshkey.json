{ "description": "Generate an ed25519 SSH keypair for a site user, install the public key in its chroot authorized_keys, and return the PRIVATE key once (not stored).",
  "timeout_seconds": 30,
  "params": { "name": { "required": true, "pattern": "[a-z][a-z0-9-]{1,30}" } } }
