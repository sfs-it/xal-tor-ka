{ "description": "Replace a site's nginx.conf (content only as env), validated with 'nginx -t' in the web container, reverted if invalid, then reloaded.",
  "timeout_seconds": 60,
  "params": { "name": { "required": true, "pattern": "[a-z][a-z0-9-]{1,30}" }, "content": { "required": true } } }
