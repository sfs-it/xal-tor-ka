{ "description": "Read a site's nginx.conf (the web container's server block). No side effects.",
  "timeout_seconds": 20, "params": { "name": { "required": true, "pattern": "[a-z][a-z0-9-]{1,30}" } } }
